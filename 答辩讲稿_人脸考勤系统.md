# 人脸识别考勤系统 — 答辩讲稿

> 按顺序讲，每一部分都写了"讲什么"和"关键技术细节"。

---

## 开场（30秒）

老师好，我做的项目是**人脸识别课堂考勤系统**。

简单说就是：上课时打开摄像头，自动识别每个学生的脸，自动记录出勤。支持手工补签、迟到自动判定、导出 Excel 报表。整套代码大概 3000 行 Python，在 58 人的班级上实测可用。

下面我先讲 V2 到 V3 的演进过程，然后分七个部分讲 V3 是怎么做的。

---

## V2 → V3 演进：为什么升级、改了什么

### V2 用的什么方案

V2 基于 dlib 库，采用 **CNN + HOG 混合策略**。

**HOG 检测器：** HOG（Histogram of Oriented Gradients，方向梯度直方图）是一种传统计算机视觉方法，不是深度学习。原理是把图像切成小格子，统计每个格子里边缘的方向分布（横的、竖的、斜的），拼成一个大向量，用 SVM 分类器判断"这格是不是人脸的一部分"。可以理解为手工定义"人脸该长什么样"的规则去匹配。

**CNN 检测器：** dlib 自带的一个浅层 CNN（约 5 层），在图像上滑动窗口，每个窗口送进网络输出"是人脸的概率"。

两者的致命短板：

| | HOG | CNN |
|------|------|------|
| 速度 | 快（0.1-0.2s/帧）| 极慢（几秒/帧）|
| 精度 | 差，只能正脸，光线一变就漏 | 好，侧脸也能扛 |
| 关键点 | 无 | 68 点但精度一般 |
| GPU 加速 | 不支持 | 几乎不支持 |

所以 V2 搞了混合策略——**注册用 CNN（求准，反正只注册一次），考勤用 HOG（求快，每帧都要跑）**。这本质上是在一个老引擎上打补丁，两个场景用两个不同的检测器。

**编码：** dlib 的 ResNet 模型输出 **128 维**向量，用三元组损失（triplet loss）训练。

**匹配：** 欧氏距离逐一比对，单一阈值 0.5。小于 0.5 就算匹配上，不管第二名离得多近。

### 这套方案的问题

1. **128 维区分度不够。** 58 人班级，不同人的向量有时候也离得不远，灰色地带大。
2. **HOG 检测太弱。** 光线变化、人侧脸就漏。而且没有关键点，后续无法做对齐。
3. **欧氏距离单一阈值。** 只看最小距离，不看和第二名的差值。距离 0.48 就确认，不考虑第二名是不是 0.49。
4. **注册没有质量意识。** 眨眼、晃动的照片和好照片权重一样。
5. **没有跟踪。** 每帧独立识别，结果可能闪烁。
6. **BGR↔RGB 反复转换。** dlib 要 RGB，OpenCV 出 BGR，PIL 渲染要 RGB，整条链路来回转。

### V3 的核心变化

V3 换了引擎——从 dlib 全面迁移到 InsightFace。不是修修补补，是重新设计整条链路：

| 维度 | V2 | V3 |
|------|-----|-----|
| 检测器 | HOG / CNN（分开用） | SCRFD（统一用，又快又准） |
| 编码器 | dlib 128 维 | ArcFace 512 维（信息量 4 倍） |
| 训练数据 | ~300 万 | 602 万（MS1M 数据集） |
| 匹配 | 欧氏距离，逐一比对 | 余弦相似度，矩阵批量匹配 |
| 阈值 | 单一阈值 0.5 | 三区 + margin 检查 |
| 人脸对齐 | 无 | 5 点仿射对齐到 ArcFace 112×112 模板 |
| 姿态估计 | 无 | roll / yaw / frontal_score |
| 注册 | jitter 5 次取平均 | 质量评分 + Top-N + 离群值剔除 + 加权平均 |
| 跟踪 | 无 | SimpleTracker（IoU + 3 帧投票） |
| 色彩 | BGR↔RGB 反复转 | 全链路 BGR |

V3 不再需要混合策略了，因为 SCRFD 本身做到了又快又准——GPU 上不到 1ms 检测一帧，比 HOG 快还比 CNN 准。注册和考勤用同一个模型，区别只在于：注册时全分辨率 + 多角度质量挑选，考勤时可缩放 + 跳帧加速。

---

## 第一部分：整体怎么跑起来的（main.py）

### 讲什么

整个程序的入口只有 94 行，做了三件事。

### 关键技术

**第一件事：注册 CUDA 加速路径（第 10-14 行）**

```python
_site_packages = os.path.join(sys.prefix, 'Lib', 'site-packages')
for _lib in ['nvidia/cudnn/bin', 'nvidia/cublas/bin']:
    _path = os.path.join(_site_packages, _lib)
    if os.path.isdir(_path):
        os.environ['PATH'] = _path + ';' + os.environ.get('PATH', '')
```

InsightFace 底层用的是 ONNX Runtime 做推理。如果电脑有 NVIDIA 显卡，需要找到 cuDNN 和 cuBLAS 的 DLL 文件。Windows 上加载 DLL 必须通过 PATH 环境变量，不能用 Python 的 `os.add_dll_directory()`——这是我踩过的坑，后者只影响 Python 的 ctypes 模块，不影响 C++ 层的动态链接。

**而且这行必须在 `import onnxruntime` 之前执行**，因为 onnxruntime 在导入时就尝试加载 GPU 驱动，晚一步就加载不上了。

**第二件事：创建 AppContext（第 79 行）**

```python
ctx = AppContext()
```

这个后面单独讲。简单理解就是把数据库、人脸模型、各种服务全部装到一个箱子里，整个程序共享。

**第三件事：登录循环（第 85-98 行）**

```python
while True:
    login_dialog = LoginDialog(ctx)
    if login_dialog.exec_() != LoginDialog.Accepted:
        break          # 点了关闭 → 退出程序
    window = MainWindow(ctx)
    window.show()
    app.exec_()
    if ctx.session.current_user is None:
        continue       # 点了退出登录 → 回到登录界面
    else:
        break          # 点了关闭 → 退出程序
```

这里用一个 `_is_logging_out` 标志位区分"退出登录"和"直接关闭窗口"。退出登录时把 current_user 清掉，回到 while 循环顶部重新弹登录框，不用重启程序。

---

## 第二部分：config.py —— 所有参数的控制面板

### 讲什么

这是一个 79 行的配置文件，项目里所有可调参数都在这里。老师如果想看不同参数的效果，改这一个文件就行。

### 关键技术

**GPU/CPU 适配的实际行为：**

`--gpu` 和 `--cpu` 参数的真实含义要澄清一下——它们改的是**模型配置**，不是推理硬件。

```python
def _get_face_model():
    # 决策 providers：只要检测到 CUDA 就用 GPU，不管性能模式
    providers = ['CPUExecutionProvider']
    if 'CUDAExecutionProvider' in ort.get_available_providers():
        providers = ['CUDAExecutionProvider', 'CPUExecutionProvider']

    # 模型名从 config 读（已被 auto_detect_profile 修改过）
    model_name = config.FACE_MODEL_NAME  # 'buffalo_l' 或 'buffalo_sc'
```

两种参数的实际行为：

| 命令 | 模型 | 检测尺寸 | 推理硬件 | GPU 占用 |
|------|------|---------|---------|---------|
| `--gpu` / 默认有独显 | buffalo_l (ResNet50, ~300MB) | 640×640 | GPU | 70-90% |
| `--cpu` / 默认只有核显 | buffalo_sc (MobileFaceNet, ~70MB) | 320×320 | GPU | ~40% |
| 纯 CPU 机器 | buffalo_sc | 320×320 | **CPU** | 0% |

**也就是说：** `--cpu` 其实是"低配模式"而非"纯 CPU 模式"。在 GPU 机器上它还是用 GPU 推理，只是换了更小的模型、更低的检测分辨率，GPU 负担从 70%+ 降到 40% 左右，适配集成显卡的电脑。只有当电脑完全没有 NVIDIA 显卡（比如只有 Intel 核显），`CUDAExecutionProvider` 检测不到，才会自动退到纯 CPU。

这个设计是合理的——GPU 天生适合做推理，40% 负载比 CPU 拉满 100% 好得多。答辩时如果在你自己的电脑上演示，`--gpu` 和 `--cpu` 的区别就是 GPU 占用率高低不同；如果在教室一台只有核显的电脑上演示，就自动走纯 CPU，速度会慢但能跑。*

**核心参数及为什么设这个值：**

| 参数 | 值 | 原因 |
|------|-----|------|
| `FACE_RECOGNITION_TOLERANCE` | 0.55（余弦相似度） | 经过反复测试，高于 0.55 基本都是同一个人，低于 0.55 基本不是。58 人班级没出现过误识 |
| `FACE_REGISTRATION_SHOTS` | 3 | 3 张够覆盖正脸、微左、微右三个角度，拍太多学生不耐烦 |
| `FACE_REGISTRATION_TOP_N` | 5 | 多张照片时只取质量最高的前 5 张，其余丢弃 |
| `FACE_REGISTRATION_MIN_QUALITY` | 0.30 | 质量低于 0.3 的（比如模糊、侧脸严重）直接拒绝参与注册 |
| `FACE_REGISTRATION_OUTLIER_SIM` | 0.30 | 某张照片的编码和聚类中心的余弦相似度低于 0.30 → 判为离群值丢弃 |
| `ATTENDANCE_COOLDOWN` | 10 秒 | 防止同一个人一直站在镜头前反复打卡 |
| `LATE_THRESHOLD_MINUTES` | 15 分钟 | 上课后 15 分钟内打卡算正常，之后算迟到 |
| `CAMERA_WIDTH/HEIGHT` | 1280×720 | 我电脑的摄像头就这个分辨率，再高也没用 |
| `ATTENDANCE_FRAME_SKIP` | 1（GPU）/ 动态（CPU） | GPU 够快所以每帧都识别 |

**外部配置覆盖（settings.json）：**

```python
import json
_CONFIG_FILE = os.path.join(BASE_DIR, 'settings.json')
if os.path.exists(_CONFIG_FILE):
    with open(_CONFIG_FILE, 'r', encoding='utf-8') as f:
        overrides = json.load(f)
    for key, value in overrides.items():
        if key.isupper() and key in globals():
            globals()[key] = value
```

不用改代码，在 `settings.json` 里写 `{"FACE_RECOGNITION_TOLERANCE": 0.50}` 就能覆盖任何参数。方便调参试验。

---

## 第三部分：人脸识别的核心实现（模型共享 + 四件套）

这是整个项目最重要的部分。在讲四个算法模块之前，先要搞清楚一个关键设计：**模型是怎么加载的、为什么 FaceDetector 和 FaceEncoder 共享同一个模型实例。**

### 3.0 模型共享机制 —— 整个系统的基石

**InsightFace 的 FaceAnalysis 对象一个东西里打包了两个模型：**

```
FaceAnalysis（一个 Python 对象，~300MB）
├── SCRFD 检测器 → 输入 BGR 图像 → 输出 bbox + kps + det_score
└── ArcFace 编码器 → 输入对齐人脸 → 输出 512 维 embedding
```

调用 `model.get(image)` 时，内部先跑 SCRFD 找到人脸，再把人脸区域送 ArcFace 编码，最后一起返回：

```python
faces = model.get(image, max_num=10)
for face in faces:
    face.bbox        # [x1, y1, x2, y2] 人脸框
    face.kps         # (5, 2) 关键点坐标
    face.det_score   # 检测置信度
    face.embedding   # 512 维特征向量（已归一化）
```

**如果创建两次会怎样？** 300MB × 2 = 600MB，显存直接爆了。所以必须保证整个程序只存在一个模型实例。

**实现方式：模块级变量 + 懒加载**

在 `face_encoder.py` 文件顶部：

```python
# 模块级变量——Python import 只执行一次，天然全局唯一
_face_model = None       # 模型实例
_model_config = {}       # 配置（模型加载前注入）

def set_model_config(model_name=None, det_size=None):
    """AppContext 最先调用，在模型加载前注入参数"""
    global _model_config
    _model_config['model_name'] = model_name  # 'buffalo_l' 或 'buffalo_sc'
    _model_config['det_size'] = det_size      # (640,640) 或 (320,320)

def _get_face_model():
    """获取模型单例——第一次调用加载，后续直接返回"""
    global _face_model
    if _face_model is None:          # ← 只进一次
        # 1. 决定推理硬件
        providers = ['CPUExecutionProvider']
        if 'CUDAExecutionProvider' in ort.get_available_providers():
            providers = ['CUDAExecutionProvider', 'CPUExecutionProvider']

        # 2. 从 _model_config 读取已注入的参数
        model_name = _model_config.get('model_name', 'buffalo_l')
        det_sz = _model_config.get('det_size', (640, 640))

        # 3. 创建唯一的模型实例
        _face_model = insightface.app.FaceAnalysis(
            name=model_name, providers=providers
        )
        _face_model.prepare(ctx_id=0, det_size=det_sz)

        # 4. 预热：跑一次假图，让 GPU 预编译 CUDA 内核
        dummy = np.zeros((112, 112, 3), dtype=np.uint8)
        _face_model.get(dummy, max_num=1)

    return _face_model   # ← 第二次及以后直接返回
```

**配置先注入、模型懒加载的时序：**

```
AppContext.__init__()
  ├── set_model_config('buffalo_l', (640,640))   ← 注入参数，但模型还没加载
  ├── self.face_detector = FaceDetector()          ← 创建对象，但模型还没加载
  ├── self.face_encoder = FaceEncoder()            ← 创建对象，但模型还没加载
  └── self.attendance_service = AttendanceService(detector, encoder, matcher)
        ↓
    用户登录 → 点"开始考勤" → 第一帧到来
        ↓
    encoder.detect_and_encode(frame)
      → _get_face_model()                          ← 此时才第一次加载模型
```

**好处：** 程序启动时不卡（import 一堆模块不会触发模型加载），配置参数在加载前就设置好了，模型只加载一次。

**FaceDetector 和 FaceEncoder 怎么共享：**

两者都调 `_get_face_model()`，拿到的是同一个 Python 对象（同一个内存地址）：

```python
# face_detector.py
from .face_encoder import _get_face_model   # ← 引同一个函数

class FaceDetector:
    def detect(self, image):
        model = _get_face_model()            # ← 拿到同一个 _face_model
        faces = model.get(small)              # 只关心 bbox
        return locations

# face_encoder.py
class FaceEncoder:
    def detect_and_encode(self, image):
        model = _get_face_model()            # ← 同一个 _face_model
        faces = model.get(small)              # 关心 bbox + embedding + kps
        return results
```

本质上是一个模型对象，两种使用方式——FaceDetector 只用它的检测能力，FaceEncoder 用它的完整能力（检测 + 编码 + 关键点）。FaceMatcher 不碰模型，只做纯数学的向量比对。

### 3.1 face_detector.py —— 找到脸在哪

**用的方法：** InsightFace 的 **SCRFD** 检测模型。

SCRFD 是一个单阶段人脸检测器，比传统的 MTCNN 快 3 倍、比 dlib HOG 准很多。它不光输出人脸框，还输出 **5 个关键点**（左眼、右眼、鼻尖、左嘴角、右嘴角），这 5 个点后面对齐和姿态估计都要用。

**具体实现：**

```python
def detect(self, image, resize_scale=None):
    scale = resize_scale or self.resize_scale
    if scale != 1.0:
        small = cv2.resize(image, (int(w * scale), int(h * scale)))
    else:
        small = image

    model = _get_face_model()        # 共享的 InsightFace 单例
    faces = model.get(small)         # SCRFD 检测

    locations = []
    for face in faces:
        x1, y1, x2, y2 = face.bbox.astype(int)
        if scale != 1.0:
            # 坐标还原回原图尺寸
            x1, y1, x2, y2 = int(x1/scale), int(y1/scale), ...
        locations.append((y1, x2, y2, x1))  # → dlib 兼容格式
    return locations
```

**关键点：** 先缩图再检测（比如 1280×720 → 640×360），检测完把坐标放大还原。相当于在缩略图上跑模型，速度更快而且对精度影响很小。

**达到的效果：** 正面人脸 100% 检测到，侧脸 30 度以内也能检到。检测一帧耗时 < 1ms（GPU）。

### 3.2 face_aligner.py —— 把脸摆正 + 评价质量

这是 V3 全新加的模块，做三件事。

**第一件事：仿射对齐**

```python
ARCFACE_TEMPLATE = np.array([
    [38.2946, 51.6963],   # 左眼在 112×112 图上的标准位置
    [73.5318, 51.5014],   # 右眼
    [56.0252, 71.7366],   # 鼻尖
    [41.5493, 92.3655],   # 左嘴角
    [70.7299, 92.2041],   # 右嘴角
])

def align_face(image, landmarks, output_size=(112,112)):
    src_pts = landmarks[:5]                       # 实际检测到的 5 个点
    M, _ = cv2.estimateAffinePartial2D(src_pts, ARCFACE_TEMPLATE)
    aligned = cv2.warpAffine(image, M, output_size)
    return aligned
```

**为什么需要对齐？** 同样一个人，歪头和正脸提取出来的特征向量可以差很远。对齐本质上是把脸上的 5 个关键点"拉到"标准位置，消除旋转、缩放、平移的差异。对齐后再给 ArcFace 编码，比不对齐准确很多。

ArcFace 模板上的坐标（比如左眼 x=38.29, y=51.70）是 InsightFace 官方训练时就定好的，不是我自己编的。用 `estimateAffinePartial2D` 算出从实际关键点映射到模板的仿射矩阵，只做旋转+缩放+平移，不做剪切和透视，因为人脸近似刚体。

**第二件事：姿态估计**

从 5 个关键点的几何关系推算头的朝向：

- **roll（侧倾）**：两眼连线和水平线的夹角。头歪了 roll 就大。
- **yaw（偏转）**：左眼到鼻子的距离 除以 右眼到鼻子的距离。脸正对着镜头时这个比值接近 1.0，脸往左转比值变大，往右转变小。
- **frontal_score（正面评分 0~1）**：综合 roll、yaw、眼距，越大越正。

```python
def estimate_pose(landmarks):
    left_eye, right_eye = pts[0], pts[1]
    nose = pts[2]

    # roll: 两眼连线角度
    dx, dy = right_eye[0] - left_eye[0], right_eye[1] - left_eye[1]
    roll = np.degrees(np.arctan2(dy, dx))

    # yaw: 左右眼到鼻子的水平距离比
    yaw_ratio = (nose[0] - left_eye[0]) / (right_eye[0] - nose[0] + 1e-6)

    # 是否正面
    is_frontal = (abs(roll) < 20° and abs(yaw_ratio-1.0) < 0.4)

    # 正面评分
    roll_score = max(0, 1.0 - abs(roll) / 45.0)       # 权重 40%
    yaw_score = max(0, 1.0 - abs(yaw_ratio-1.0)/0.8)  # 权重 40%
    size_score = min(1.0, eye_distance / 60.0)         # 权重 20%
    frontal_score = roll_score*0.4 + yaw_score*0.4 + size_score*0.2
```

**第三件事：综合质量评估（4维加权）**

```python
def assess_quality(image, landmarks, det_score):
    # 姿态 (40%)
    pose = estimate_pose(landmarks)

    # 清晰度 (30%) — 拉普拉斯算子
    gray = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)
    laplacian_var = cv2.Laplacian(gray, cv2.CV_64F).var()
    sharpness = min(laplacian_var / 500.0, 1.0)

    # 亮度 (15%)
    brightness = min(gray.mean() / 200.0, 1.0)

    # 检测置信度 (15%) — SCRFD 给的
    total_score = (pose['frontal_score']*0.4 + sharpness*0.3
                   + det_score*0.15 + brightness*0.15)
```

输出的 `is_frontal` 和 `reject_reason` 可以告诉上层"为什么这张照片不行"——是头太歪了、太模糊了、还是光线太差了。

**达到的效果：** 注册时自动过滤侧脸、模糊、光线差的照片。考试验证时过滤掉质量不够的人脸不参与比对，降低误识风险。

### 3.3 face_encoder.py —— 把脸变成数字

**用的方法：** InsightFace 的 **ArcFace (ResNet50)**，输出 512 维归一化向量。

ArcFace 是目前主流的人脸识别模型之一。它的核心思想是：在训练时加一个"加性角度边际损失"（Additive Angular Margin Loss），让同一个人的特征向量在超球面上尽可能聚在一起，不同人的尽量推开。

**模块级模型单例：**

```python
_face_model = None

def _get_face_model():
    global _face_model
    if _face_model is None:
        import insightface
        providers = ['CPUExecutionProvider']
        if 'CUDAExecutionProvider' in ort.get_available_providers():
            providers = ['CUDAExecutionProvider', 'CPUExecutionProvider']
        _face_model = insightface.app.FaceAnalysis(
            name='buffalo_l',        # ResNet50 backbone
            providers=providers
        )
        _face_model.prepare(ctx_id=0, det_size=(640, 640))
    return _face_model
```

FaceDetector 和 FaceEncoder 共享这一个单例。因为同一个 InsightFace 模型里就同时包含了 SCRFD 检测器和 ArcFace 编码器，不需要加载两次（省约 300MB 显存）。

**核心方法：detect_and_encode() —— 一步完成检测+编码**

```python
def detect_and_encode(self, image, max_num=10, resize_scale=None):
    # 缩放
    small = cv2.resize(image, ...)

    # 一次 model.get() 同时完成检测+编码+5点关键点
    model = _get_face_model()
    faces = model.get(small, max_num=max_num)

    results = []
    for face in faces:
        emb = face.embedding                    # 512维 ArcFace 向量
        emb = emb / np.linalg.norm(emb)         # L2 归一化到单位球面

        landmark = face.kps                     # 5点关键点
        pose = FaceAligner.estimate_pose(landmark)
        quality = FaceAligner.assess_quality(face_region, landmark, det_score)
        aligned_face = FaceAligner.align_face(image, landmark)

        results.append({
            'location': (top, right, bottom, left),
            'encoding': emb,
            'det_score': det_score,
            'landmark': landmark,
            'pose': pose,
            'quality': quality,
            'aligned_face': aligned_face,
        })
    return results
```

**为什么这一步很重要？** 如果分离做——先调 detect() 找到人脸框，再 crop 出来调 encode()——相当于同一个模型前向传播了两次。一步做完就只跑一次前向传播，省一半推理时间。

**注册用的质量加权编码：encode_with_quality()**

这是注册流程的核心，分 6 步：

```
第1步：每张照片 detect_and_encode → 得到编码 + 质量分 + 姿态
第2步：过滤质量 < 0.30 的照片（模糊、侧脸严重的丢弃）
第3步：按质量排序，只取最好的前 5 张（TOP_N = 5）
第4步：离群值剔除
      → 算所有编码的均值中心
      → 每个编码和中心的余弦相似度
      → 相似度 < 0.30 的视为离群值，丢弃
第5步：质量加权平均
      → 每张照片的权重 = 它的质量分 / 所有质量分之和
      → 加权求和
第6步：L2 归一化输出
```

具体代码（第 4 步离群值剔除）：

```python
encodings = np.array([e for e, _ in valid])
mean_enc = np.mean(encodings, axis=0)
mean_enc = mean_enc / np.linalg.norm(mean_enc)

sims = encodings @ mean_enc       # 每个编码与中心的余弦相似度
mask = sims > 0.30                # 只保留相似度 > 0.30 的
if mask.sum() == 0:
    # 全是离群值？至少保留最好的一个
    best_idx = np.argmax(sims)
    mask[best_idx] = True
```

第 5 步质量加权平均：

```python
weights = np.array([score for _, score in filtered])
weights = weights / weights.sum()

weighted_avg = np.zeros(512)
for enc, w in zip(enc_list, weights):
    weighted_avg += w * enc    # 好照片贡献大，差照片贡献小
weighted_avg = weighted_avg / np.linalg.norm(weighted_avg)
```

**为什么不用简单的等权平均？** 假设拍了 3 张，其中 1 张人刚好眨眼睛或者晃动了。等权平均 = 每张权重 33%，那张烂照片会把整体编码拉偏。质量加权的话，烂照片权重可能只有 10%，好照片权重 45%，结果准确很多。

**达到的效果：** 同一个人不同角度、不同光线拍 3 张，加权编码后和这个人其他照片的余弦相似度明显高于简单平均。58 个人的库，用加权注册后识别准确率比简单平均高。

### 3.4 face_matcher.py —— 这个数字是谁

**用的方法：余弦相似度 + 预计算矩阵 + 三区阈值 + margin 检查。**

**为什么是余弦相似度？** ArcFace 训练时用的就是余弦距离，模型输出的向量已经 L2 归一化到单位球面上了。所以用余弦相似度（即两个归一化向量的内积）是最匹配的度量。相比之下，欧氏距离在 512 维空间里区分度不如余弦相似度。

**预计算矩阵 —— O(1) 匹配：**

```python
def update_database(self, encodings, names, student_ids):
    # 把全库的编码堆成矩阵
    self._known_matrix = np.array(encodings, dtype=np.float32)  # (N, 512)
    # 批量 L2 归一化（只做一次）
    norms = np.linalg.norm(self._known_matrix, axis=1, keepdims=True)
    self._known_matrix = self._known_matrix / (norms + 1e-10)
    self._known_names = names
    self._known_ids = student_ids
```

刷新人脸库时一次性做好归一化。之后每次匹配只需要：

```python
def match(self, face_encoding):
    query = face_encoding / np.linalg.norm(face_encoding)   # (512,)
    similarities = self._known_matrix @ query                # (N,512) @ (512,) → (N,)
    best_idx = np.argmax(similarities)
    best_sim = similarities[best_idx]
```

这是一个矩阵-向量乘法。numpy 底层调用 BLAS 库（在 GPU 上是 cuBLAS），对 58 人的库来说几乎是瞬间完成。而 V2 用 dlib 时是逐个算欧氏距离（O(N)），58 人要循环 58 次。

**批量匹配 —— 一次处理多人：**

```python
def match_multiple_batch(self, face_encodings):
    queries = np.array(face_encodings, dtype=np.float32)   # (M, 512)
    queries = queries / np.linalg.norm(queries, axis=1, keepdims=True)
    sim_matrix = queries @ self._known_matrix.T             # (M, N)
    # 对每行找最大值 → 每个人的最佳匹配
```

一帧里有 5 个人，一次 `(5,512) @ (512,58) → (5,58)` 矩阵乘法搞定全部。比循环 5 次快很多。

**三区阈值 + margin 策略：**

```python
def _classify_match(self, best_sim, margin):
    if best_sim >= 0.55:
        # 高置信区：直接确认
        return True, float(best_sim)
    elif best_sim >= 0.42:
        # 不确定区：需要 margin ≥ 0.08 才确认
        if margin >= 0.08:
            return True, float(best_sim * 0.9)    # 确认但降置信度
        else:
            return False, float(best_sim)          # margin不够，拒绝
    else:
        # 拒绝区：直接拒绝
        return False, float(best_sim)
```

**margin 是什么？** 最佳匹配和次佳匹配的相似度差值。

```
例子1：张三 sim=0.62，次佳李四 sim=0.35 → margin=0.27 → 确认 ✅
例子2：张三 sim=0.50，次佳李四 sim=0.48 → margin=0.02 → 拒绝 ❌
```

例子 2 中最佳匹配和次佳匹配只差 0.02，说明库里有两个人和当前人脸都很像，分不清到底是谁。这时候宁可拒绝也不冒误识的风险。

**为什么设置 0.55 这个阈值？** 这是反复试验出来的。58 人的班级里：
- 同一个人不同照片的相似度一般在 0.55-0.80 之间
- 不同人的相似度一般在 0.15-0.42 之间
- 0.42-0.55 之间是灰色地带，需要 margin 辅助判断

**达到的效果：** 58 人库里，正面光照正常的情况下几乎不会误识。偶尔有人脸质量太差的情况会被拒绝（返回 Unknown），可以后面手动补签。

---

## 第四部分：数据库怎么设计的（database/）

### 讲什么

4 张表，用 SQLAlchemy ORM 管，底层是 SQLite。

### 关键技术

**4 张表和它们的关系：**

```
students（学生表）
  ├── student_id (学号，唯一)
  ├── name (姓名)
  ├── class_name (班级)
  ├── face_encoding (BLOB，512维float32序列化，2048 bytes)
  └── created_at

courses（课程表）
  ├── course_code (课程代码)
  ├── course_name (课程名)
  ├── teacher_name (教师)
  ├── start_time (上课时间，如 "08:00")
  └── end_time (下课时间)

attendance_records（考勤记录表）
  ├── student_id (外键 → students.id)
  ├── course_id (外键 → courses.id)
  ├── check_time (打卡时间)
  ├── status (normal / late / absent)
  ├── confidence (识别置信度)
  └── remark (备注)

users（用户表）
  ├── username (唯一)
  ├── password_hash (bcrypt 哈希)
  └── role (admin / teacher)
```

**4 个复合索引** 加在 `attendance_records` 上：

```python
Index('idx_attendance_student_time', 'student_id', 'check_time'),  # 查某人某天打没打卡
Index('idx_attendance_course', 'course_id'),                        # 查某门课的所有记录
Index('idx_attendance_check_time', 'check_time'),                   # 按日期范围查询
Index('idx_attendance_status', 'status'),                           # 按状态筛选（正常/迟到）
```

因为考勤记录是查询最频繁的表——每次加载考勤面板都要查、导出 Excel 要查、统计出勤率要查。不加索引的话 58 人 × 每天多次打卡记录会越来越慢。

**编码存储的向后兼容：**

```python
def blob_to_encoding(blob):
    if len(blob) == 2048:        # 512 × 4 = ArcFace float32
        return np.frombuffer(blob, dtype=np.float32)
    if len(blob) == 1024:        # 128 × 8 = dlib float64（V2 遗留）
        return np.frombuffer(blob, dtype=np.float64)
    # 其他情况尝试 float32
```

自动识别 V2 的 128 维 dlib 编码和 V3 的 512 维 ArcFace 编码。从 V2 升级不需要清库。

**班级过滤查询：**

```python
def get_all_face_encodings(self, class_name=None):
    q = session.query(Student).filter(Student.face_encoding.isnot(None))
    if class_name:
        q = q.filter(Student.class_name == class_name)
```

考勤面板选了特定班级时，人脸库只加载那个班的学生。好处是：减少匹配候选（从 58 人降到 10-20 人），margin 更准确，也不太会出现跨班撞脸。

**学号自然排序：**

```python
def _natural_key(s):
    return [int(t) if t.isdigit() else t.lower() for t in re.split(r'(\d+)', s)]
```

因为 "1-10" 按字典序会排在 "1-2" 前面，自然排序修正了这个问题。

---

## 第五部分：考勤流程怎么串起来的（services/）

### 5.1 AppContext —— 依赖注入

```python
class AppContext:
    def __init__(self):
        self.db = DatabaseManager()           # 唯一数据库实例
        self.session = SessionManager()       # 登录会话

        # 注入配置到模型（在加载前）
        set_model_config(model_name=..., det_size=...)

        # 人脸识别核心（共享实例）
        self.face_detector = FaceDetector()
        self.face_encoder = FaceEncoder()
        self.face_matcher = FaceMatcher()

        # 服务层（注入依赖）
        self.student_service = StudentService(self.db, self.face_detector, self.face_encoder)
        self.attendance_service = AttendanceService(self.db, self.face_detector,
                                                     self.face_encoder, self.face_matcher)
        self.course_service = CourseService(self.db)
        self.auth_service = AuthService(self.db)
```

**AppContext 中的依赖注入关系：**

```
AppContext（集中容器，main.py 中创建一次）
  │
  ├── db: DatabaseManager ───────────────→ 全程序唯一 SQLite 连接
  ├── session: SessionManager ───────────→ 当前登录用户
  │
  ├── face_detector: FaceDetector ────┐
  ├── face_encoder: FaceEncoder ──────┤── 共享同一个 InsightFace 实例
  ├── face_matcher: FaceMatcher ──────┘   (纯数学，不依赖模型)
  │
  ├── auth_service ─────── 依赖 db
  ├── course_service ───── 依赖 db
  ├── student_service ──── 依赖 db + face_detector + face_encoder
  └── attendance_service ─ 依赖 db + face_detector + face_encoder + face_matcher
       │
       └── SimpleTracker ── 依赖 IoU + 投票（纯逻辑，不依赖模型）
```

所有 GUI 界面通过 `self.ctx.xxx` 访问，不再自己 new 实例。

### 5.2 考勤识别流程（attendance_service.py）

这是整个系统最核心的业务逻辑。**完整的端到端调用链：**

```
① 用户点"开始考勤"
    ↓
② AttendancePanel.start_camera()
   → CameraThread(1280×720, 30fps).start()    ← 采集线程
   → FaceRecognitionThread(attendance_service).start()  ← 识别线程
    ↓
③ CameraThread 循环产生帧
   cv2.VideoCapture.read() → BGR 帧
   → cv2.flip(1) 镜像翻转
   → frame_ready 信号 → UI 线程
    ↓
④ UI 线程 AttendancePanel.on_frame_ready()
   → _frame_counter++
   → 每 N 帧 (GPU=1): recognition_thread.add_frame(frame_copy)
   → draw_results(frame)  ← 直接显示画面，不等待识别
    ↓
⑤ 识别线程 FaceRecognitionThread.run()
   → frame = queue.get()
   → self.attendance_service.process_frame(frame)
        │
        ├→ encoder.detect_and_encode(frame, max_num=10)
        │     → _get_face_model()          ← 首次调用：加载 300MB 模型 + GPU 预热
        │     → model.get(small)            ← SCRFD + ArcFace 一次完成
        │     → 返回 [{encoding, location, landmark, pose, quality}, ...]
        │
        ├→ matcher.match_multiple_batch(encodings)
        │     → queries @ _known_matrix.T   ← (M,512)@(512,N) 一次矩阵乘法
        │     → 每人的 (name, confidence, index)
        │
        └→ tracker.update(raw_detections)
              → IoU 贪心匹配关联帧间同一人脸
              → 3 帧滑动窗口投票
              → 输出稳定结果
        │
   → result_ready 信号 → UI 线程
    ↓
⑥ UI 线程 on_recognition_result()
   → 更新 last_results（有结果才更新，空的不覆盖 → 不闪）
   → 对每个识别到的人: check_in(student_id, course_id, confidence)
        → 冷却检查：上次打卡 < 10 秒？跳过
        → 已打卡检查：今天打过了？跳过
        → 迟到判定：当前时间 > 课程开始 + 15 分钟？late : normal
        → 写入 attendance_records 表
   → refresh_records()
        → 加载今日记录
        → 去重：按(student_id, name) 去重
        → 出勤率 = 去重已打卡人数 / 全班总人数
        → 更新界面统计标签
```

一帧图像进来的处理流程：

```
原始帧 BGR (1280×720)
    ↓
① encoder.detect_and_encode(frame, max_num=10)
   一次调用完成 SCRFD检测 + ArcFace编码 + 5点关键点 + 姿态 + 质量评估
   输出：最多10个人的编码结果列表
    ↓
② matcher.match_multiple_batch(encodings)
   一次矩阵乘法 (M,512) @ (512,N) 完成所有人和全库的匹配
   输出：每个人的 (姓名, 置信度, 库内索引)
    ↓
③ SimpleTracker.update(raw_detections)
   IoU匹配关联帧间同一人脸 + 滑动窗口投票平滑
   输出：消除闪烁后的稳定识别结果
    ↓
④ check_in(student_id, course_id, confidence)
   冷却检查（10秒内同一人不再打卡）
   迟到判定（当前时间 > 课程开始 + 15分钟？）
   写入 attendance_records 表
    ↓
⑤ UI 更新
   考勤面板刷新右侧统计（已打卡人数/总人数）
   视频画面画人脸框 + 姓名标签
```

**跟踪器（SimpleTracker）详解：**

```python
class SimpleTracker:
    def update(self, detections):
        # 第1步：贪心 IoU 匹配
        for each detection:
            找和已有 track 中 IoU 最大的那个
            如果 IoU > 0.3 → 关联上，更新 track 的 bbox 和 votes

        # 第2步：未匹配的检测 → 新建 track
        for unmatched detection:
            创建新 track，votes 初始化为当前名字

        # 第3步：未匹配的 track → lost_count++
        # 第4步：lost_count > 3 → 删除 track（人走出了画面）

        # 第5步：投票平滑
        for each track:
            votes = track['votes']         # 最近3帧的识别名
            counter = Counter(votes)        # {'张三':2, '李四':1}
            if 最高票 >= 2票 且 > 50%:
                输出最高票的名字
            else:
                输出最新帧的名字
```

**举个例子理解投票的作用：**
```
帧1: 检测到一个人 → ArcFace 输出"张三" sim=0.62
帧2: 同一个人（IoU 匹配上了）→ 输出"张三" sim=0.58
帧3: 同一个人 → 输出"李四" sim=0.42（单帧误识别）

没有跟踪器: UI 上第3帧名字闪成"李四"，看起来在跳
有跟踪器: Counter = {'张三':2, '李四':1} → 2票 > 50% → 输出"张三" ✅
```

**冷却去重：**

```python
def check_in(self, student_id, course_id):
    current_time = time.time()
    if student_id in self._last_check_time:
        if current_time - self._last_check_time[student_id] < 10:
            return False, 'cooldown', f"请等待 {remaining} 秒"
    
    if self.db.check_today_attendance(student['id'], course_id):
        return False, 'duplicate', f"已打卡"    # 今天打过了
    
    # 迟到判定
    if 当前时间 > 课程开始时间 + 15分钟:
        status = 'late'
    else:
        status = 'normal'
    
    self.db.add_attendance_record(...)
```

**出勤率正确计算：**

```python
# V3 的正确做法（在 attendance_panel.py 里）
total_students = len(全班学生)
checked_in = len(去重已打卡学生)
attendance_rate = checked_in / total_students * 100
```

之前 V2 用"打卡记录数/学生总人数"算，举个反例：3 个人站在镜头前反复打卡 100 次，V2 算出勤率 100/3 = 3333%，显然是错的。现在按去重人头算。

### 5.3 手动补签（makeup_checkin_dialog.py）

```python
def manual_makeup_check_in(self, student_id, course_id, status, remark):
    # 绕过人脸识别和冷却时间
    student = self.db.get_student(student_id)

    if self.db.check_today_attendance(student['id'], course_id):
        return False, f"已打卡"     # 打过的不能再补

    full_remark = f"手动补签" if not remark else f"手动补签 - {remark}"
    self.db.add_attendance_record(
        student_id=student['id'],
        course_id=course_id,
        status=status,           # 教师选的正常/迟到
        confidence=None,         # 手动补签没有置信度
        remark=full_remark       # 备注自动加"手动补签"前缀
    )
```

界面功能：列出今日所有未打卡学生 → 按班级/学号/姓名搜索 → 勾选 → 选状态 → 批量提交。

### 5.4 学生注册流程（student_service.py）

```python
def register_student(self, student_id, name, class_name, images):
    # 调用 encoder 的质量加权方法
    avg_encoding = self.encoder.encode_with_quality(images)

    # 如果质量编码失败，回退到简单平均
    if avg_encoding is None:
        avg_encoding = self.encoder.encode_average(images)

    self.db.add_student(student_id, name, class_name, avg_encoding)
```

批量导入支持中断续传：`register_from_directory()` 读取文件夹 → 每张照片都跑质量加权 → 已注册的跳过 → 失败的写错误日志。

---

## 第六部分：界面怎么做的（gui/）

### 讲什么

PyQt5 桌面应用，Apple 风格设计。主界面是侧边栏导航 + 右侧内容切换。

### 关键技术

**界面结构：**

```
┌─────────────┬──────────────────────────────────┐
│  侧边栏      │   QStackedWidget (切换面板)       │
│             │                                  │
│  [考勤打卡]  │   考勤面板 (AttendancePanel)       │
│  [学生管理]  │   或                               │
│  [课程管理]  │   教师面板 (TeacherPanel)           │
│  [考勤记录]  │                                  │
│  [数据导出]  │                                  │
│             │                                  │
│  用户信息    │                                  │
│  [退出登录]  │                                  │
└─────────────┴──────────────────────────────────┘
```

**考勤面板（attendance_panel.py）—— 双线程 + 显示优化：**

```
主线程（UI）
  ├── CameraThread(QThread, 30fps)
  │     → frame_ready 信号 → 更新视频画面
  │     → 每 N 帧 → add_frame() 送给识别线程
  │
  └── FaceRecognitionThread(QThread)
        → 从队列取帧
        → attendance_service.process_frame()
        → result_ready 信号 → UI 画框 + 打卡 + 更新统计
```

**显示性能优化（三个缓存）：**

1. **结果不变零开销**：识别结果和上一帧一样 → 直接复用缓存的渲染图，不重新 `cv2.rectangle()` + PIL 画文字
2. **内存地址复用**：frame 数据指针没变 → 跳过 `QImage` 构造 + `scaled()`
3. **全链路 BGR**：摄像头输出是 BGR 格式，直接在上面用 cv2 函数画框（BGR 原生）。只有文字标签用 PIL 渲染（几十像素的小区域），PIL 输出 RGBA → 翻转通道到 BGR → alpha 混合到原帧。**省掉了全帧两次 cvtColor（BGR→RGB→BGR）**

**BGR 全链路渲染（V2 vs V3 的关键性能优化）：**

V2 的渲染管线为什么必须反复转换：

1. dlib 输入要求 RGB → OpenCV 出 BGR，必须 `cvtColor(BGR→RGB)` 给 dlib
2. PIL 的原生色彩空间是 RGB → 整帧送 PIL 渲染文字标签
3. OpenCV 画框函数是 BGR → 又得 `cvtColor(RGB→BGR)` 给 cv2 画框
4. PyQt5 QImage 默认 RGB → 又得转

```
V2: 摄像头BGR → cvtColor(RGB) → dlib识别 → PIL全帧渲染(1280×720,RGB)
     → cvtColor(BGR) → cv2画框 → QImage(RGB888) → 显示
     ↑ 两次全帧 cvtColor，1280×720×3通道 = 约 2.7M 像素每次
```

V3 为什么能全链路 BGR：

1. **InsightFace 原生接受 BGR** — 不需要转
2. **cv2.rectangle 原生 BGR** — 直接在帧上画框，颜色 `(0,255,0)` 就是 BGR 的绿色
3. **QImage 支持 Format_BGR888** — 直接吃 BGR 数据，内部自己搞定显示转换
4. **文字标签只处理小区域** — 只在几十像素宽的名字标签上用 PIL，渲染完从 RGBA 翻转通道到 BGR（`label_rgba[:,:,2::-1]`），然后 alpha 混合回原帧

```python
# attendance_panel.py 第 673-709 行
def _draw_label(self, frame, left, top, name, confidence, color):
    label = f"{name} {confidence:.0%}"

    # PIL 只渲染几十像素宽的标签（不是全帧！）
    label_img = Image.new('RGBA', (tw + pad*2, th + pad*2))
    draw.text([...], label, font=font, fill=(255,255,255,255))

    # RGBA → numpy → 翻转通道到 BGR
    label_rgba = np.array(label_img)
    label_bgr = label_rgba[:, :, 2::-1]   # RGBA → BGR
    alpha = label_rgba[:, :, 3:4] / 255.0

    # alpha 混合到原帧的小区域
    roi = frame[y1:y2, x1:x2]
    frame[y1:y2, x1:x2] = (label_bgr * alpha + roi * (1 - alpha))

# display_image: 用 Format_BGR888 直接显示 BGR（第 749 行）
q_image = QImage(image.data, w, h, bytes_per_line, QImage.Format_BGR888)
```

```
V3: 摄像头BGR → InsightFace直接吃BGR → cv2在BGR上画框
     → PIL只渲染标签(约200×30像素,RGBA→BGR翻转)
     → alpha混合回BGR → QImage(Format_BGR888)直接显示
     ↑ 0 次全帧 cvtColor
```

三个条件缺一不可：InsightFace 接受 BGR + cv2 原生 BGR + QImage 支持 BGR888。V2 做不到是因为被 dlib 的 RGB 要求绑死了整个链路。

**教师面板（teacher_panel.py）—— 后台导入：**

批量导入学生照片时使用 `ImportWorker(QThread)` 在后台线程跑：

```python
class ImportWorker(QThread):
    progress_signal = pyqtSignal(int, int, str)   # 当前/总数/状态
    finished_signal = pyqtSignal(int, list, str)   # 成功数/错误列表/日志路径

    def run(self):
        for each photo:
            if self._cancelled: break
            register_student(...)  # 质量加权编码
            progress_signal.emit(current, total, name)
```

这样导入 50 个人的照片（每人 3 张 = 150 张），界面不会卡住，进度条实时更新。

**Apple 风格设计系统（apple_style.py）：**

所有颜色、圆角、按钮样式、表格样式都定义在一个文件里：

```python
COLORS = {
    'primary': '#007AFF',      # 苹果蓝
    'success': '#34C759',      # 苹果绿
    'danger': '#FF3B30',       # 苹果红
    'background': '#F5F5F7',   # 苹果灰背景
    'sidebar': '#1D1D1F',      # 深色侧边栏
    ...
}
```

各界面调 `get_button_style('primary')` 拿到统一的按钮 CSS。想换主题色只改这一个文件。

---

## 第七部分：工具箱（utils/）

简要说明即可。

| 文件 | 做什么 | 为什么要单独抽出来 |
|------|--------|-------------------|
| camera.py | 30fps 读取摄像头，发 frame_ready 信号 | UI 线程不能阻塞 |
| excel_exporter.py | 考勤记录导出 .xlsx（用 openpyxl） | 格式固定，独立模块 |
| security.py | bcrypt 密码哈希/验证 | V2 时密码逻辑在 auth_service 里，db_manager 要 import 它导致环形引用。抽出来后不再互相依赖 |
| logger.py | 统一日志格式，输出到文件和控制台 | 调试方便 |
| exceptions.py | 11 个自定义异常类 | 细粒度错误处理 |

---

## 总结：整个项目的数据流一句话

> **config 定规则 → core（SCRFD 检测 + 对齐摆正 + ArcFace 512维编码 + 余弦相似度匹配）做算法 → database（4表 + 4索引）存数据 → services（跟踪器投票 + 冷却去重 + 迟到判定）串流程 → gui（双线程 + BGR 全链路渲染 + 三级缓存）展示交互 → AppContext 把一切串起来**

---

## 快速参考：评委可能问的问题

**Q: 为什么选 InsightFace 不选 dlib？**
A: dlib 只输出 128 维向量，区分度不够。InsightFace 的 ArcFace 输出 512 维，58 人班级区分很稳。而且 SCRFD 检测器比 dlib HOG 快 2-3 倍还更准。

**Q: 为什么用余弦相似度不用欧氏距离？**
A: ArcFace 训练时就用的余弦距离（角度空间）。输出向量已经 L2 归一化，此时余弦相似度 = 向量内积，计算比欧氏距离简单而且和训练目标一致。

**Q: 阈值 0.55 怎么定的？**
A: 反复试验。同一个人的不同照片 sim 一般 0.55-0.80，不同人 0.15-0.42。0.55 是分界线。

**Q: 误识别过吗？怎么处理的？**
A: 有 margin 机制兜底。最佳和次佳 sim 差不到 0.08 的会拒绝，宁可漏识不误识。漏了可以手动补签。

**Q: 注册为什么要拍好几张？**
A: 单张可能存在眨眼、晃动、光线差的问题。多张 + 质量评分 + 离群值剔除 + 加权平均得到更稳定的模板。

**Q: 出勤率怎么算的？**
A: 去重已打卡人数 / 全班学生总数。不是打卡记录次数/总人数（那会超过 100%）。

**Q: 支持多少人同时识别？**
A: 理论不设上限，每次 `detect_and_encode(max_num=10)` 返回最多 10 人。实际教室前排能同时拍到的最多 5-6 人。

**Q: 数据库为什么用 SQLite？**
A: 桌面单机应用，SQLite 零配置、零依赖、一个文件搞定。SQLAlchemy ORM 让以后想换 MySQL/PostgreSQL 改一行配置就行。

**Q: 遇到的最大困难是什么？**
A: GPU 推理环境配置。Windows 上 cuDNN DLL 必须通过 PATH 环境变量加载，而且 `import onnxruntime` 必须在 `import PyQt5` 之前，不然 DLL 冲突。试了很多次才找到正确顺序。
